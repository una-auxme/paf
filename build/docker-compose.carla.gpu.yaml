services:
  carla-simulator:
    extends:
      file: docker-compose.carla.base.yaml
      service: carla-simulator
    devices:
      - /dev/dri

  carla-ros-bridge:
    extends:
      file: docker-compose.carla.base.yaml
      service: carla-ros-bridge

networks:
  carla:
