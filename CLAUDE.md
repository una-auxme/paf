# Claude Code Instructions

Use `agents.md` as the canonical repository instruction source.

For non-trivial work, also read:

- `.agent/PLANS.md` for plan structure and proof obligations
- `doc/dev_talks/paf25/future_work.md` for the recommended execution order
- `doc/dev_talks/paf25/improvements_assessment.md` for the consolidation-first development direction

When work produces progress or handoff notes that should preserve the current chat state, store them as timestamped files under `doc/dev/progress/` and keep them discoverable via `doc/dev/README.md` and the relevant documentation index.

When work produces deeper analysis, comparisons, or longer-form reasoning that should survive the chat session, store them under `doc/reasoning/` and keep the canonical behavior documentation in the appropriate `doc/` or package folder.
